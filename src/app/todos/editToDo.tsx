import { ActionIcon } from '@mantine/core';
import { IconEdit } from '@tabler/icons-react';

const editToDo = () => {
  return (
    <ActionIcon>
      <IconEdit></IconEdit>
    </ActionIcon>
  );
};

export default editToDo;
